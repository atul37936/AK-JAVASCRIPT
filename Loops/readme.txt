Easy Problems (1-6)
Print numbers 1 to 10 – Use a for loop.

Print even numbers from 1 to 20 – Use any loop.

Sum of first N natural numbers – Input N, calculate sum.

Multiplication table – Input a number, print its table (1 to 10).

Count digits – Input a number, count how many digits it has.

Factorial – Input a number, compute its factorial (using loop).

Reverse Counting - extra

Reverse Even Counting - extra

Reverse odd Counting - extra

odd Numbers - extra

1 to 10  Total (Sum) - extra

The "Special" Sum
Run a loop through all numbers from 1 to 20, but calculate the sum (total) of only those numbers that are divisible by 5 (e.g., 5, 10, 15, 20). - extra



Medium Problems (7-12)

Reverse a number – Input a number, output its reverse (e.g., 123 → 321).

Check palindrome – Input a number/string, check if it reads the same forwards/backwards.

Fibonacci series – Print first N terms of Fibonacci series.

GCD of two numbers – Find greatest common divisor using Euclidean algorithm (loop).

LCM of two numbers – Find least common multiple.

Prime number check – Input a number, determine if it's prime.




Hard Problems (13-18)
Print patterns – Print patterns like:

text
*
**
***
****
(Nested loops)

Armstrong number – Check if a number equals sum of cubes of its digits (e.g., 153).

Perfect number – Check if a number equals sum of its proper divisors.

Pyramid pattern – Print a centered pyramid of stars.

Number guessing game – Generate a random number, let user guess (loop until correct).

Menu-driven calculator with loop – Keep showing menu until user chooses exit (use do...while).

Bonus Challenges
Print all prime numbers up to N – Use nested loops.

Find factors of a number – Print all factors.

Convert decimal to binary – Use loop to repeatedly divide by 2.

Sum of digits until single digit – Repeatedly sum digits until result < 10.









3175 · Number of tuples that form a triangle

Input a positive integer n, and output the number of tuples of triples with elements up to n that can form the lengths of the three sides of a triangle.